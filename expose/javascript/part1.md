1. values added: 20
2. final result: 20
3. Firstly you should use let and const for variables by modern JS best practices. Secondly           
4. values added: 20
5. It returns an error "result is not defined" because when you use the let keyword it is only able to be referenced in the same block and line 13 is outside of the block that line 5 is in.
6. It returns an error, as you can't assign a new value to the const variable.
7. It returns an error, as you can't assign a new value to the const variable.